[pytest]
addopts =
    -v
    --html=reports/report.html
    --self-contained-html

testpaths = tests

markers =
    tc_id(id): Test Case ID
    priority(level): Test Priority
    title(name): Test Title
